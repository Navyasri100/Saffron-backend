# Spring Boot Profile - choose between 'local' and 'prod'
# LOCAL: mvn spring-boot:run (uses application-local.properties)
# PROD: deployed on Render (uses application-prod.properties)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# Application name
spring.application.name=restaurant-app
